# In lecture-9 we did a assignment on contact us, No the same assigment with the learning of lecture 10
1. created a folder
2. -- npm init -y
   -- npm install nodemon
   -- npm install express
   -- in package.json add "start" : "nodemon app.js"
3. -Files used and their usage
      -- utils : For absolute path
      -- views : for htmls files